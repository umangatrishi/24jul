#include <bits/stdc++.h>

using namespace std;

int main() {
    // your code goes here
    int t;
    cin >> t;
    while (t--) {
        int N,X,Y;
        cin>>N>>X>>Y;
        if(N>=X+2*Y)
        std::cout << "YES" << std::endl;
        else
        std::cout << "NO" << std::endl;
    }
}
